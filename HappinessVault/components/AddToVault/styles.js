import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '95%',
    height: '50%',
    margin: 5,
  },

  title: {
    fontSize: 20,
  },

  input: {
    height: '80%',
    margin: 12,
    borderWidth: 1,
    padding: 10,
    fontSize: 18,
  },
});

export default styles;
